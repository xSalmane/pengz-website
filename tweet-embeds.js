// tweet-embeds.js
// Paste your blockquote HTML (or tweet URLs, see below) in this array.
// Example blockquote:
// `<blockquote class="twitter-tweet" data-media-max-width="560">...</blockquote>`

const TWEET_BLOCKQUOTES = [``, ``, ``];

// For browser global usage
window.TWEET_BLOCKQUOTES = TWEET_BLOCKQUOTES;
